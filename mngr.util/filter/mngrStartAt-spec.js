describe('mngrStartAt', function() {

	beforeEach(module('mngr.util'));

	it('should ...', inject(function($filter) {

        var filter = $filter('mngrStartAt');

		expect(filter('input')).toEqual('output');

	}));

});